#include "push_swap.h"

void main_algo( int *s_a, int *s_b, int *len_a, int *len_b)
{
	print_stacks(s_a, s_b, *len_a, *len_b);
	
	//push until 3 left
	//if > 5 then calc cost and push
	//3sort
	//push back
	//rotate if needed
		
	free(s_a);
	free(s_b);
}
